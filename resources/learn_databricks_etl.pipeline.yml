# The main pipeline for learn_databricks

resources:
  pipelines:
    learn_databricks_etl:
      name: learn_databricks_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/learn_databricks_etl"

      libraries:
        - glob:
            include: ../src/learn_databricks_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
